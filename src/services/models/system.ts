export interface MobileProps {
    DeviceDetails: {
        AppVersion: string;
        Platform: string;
        PlatformOs: string;
        DeviceVersion: string;
        IpAddress: string;
        MacAddress: string;
    }
}